# Long-running bugs

This folder is for **open, recurring bugs** (symptoms, what we tried, what is still unknown). It is not the agent runbook.

Agent how-tos stay in `docs/agents/`. Point here from gotchas when a gotcha is actually an unsolved bug.

| Bug | Status |
|---|---|
| [HLAE Steam-online hook](hlae-steam-online-hook.md) | Open — mitigations in `scripts/hook_aware.py`, flake remains |
