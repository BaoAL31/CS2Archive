# Single player identity store

Recognised Pros (and FACEIT recent/popular) read `.data/player_accounts.json` only. The separate nickname watchlist `faceit_pros.json` was deleted — it was a redundant subset of accounts and forced dual maintenance. Kill Timeline matching uses Steam IDs from the same file.
